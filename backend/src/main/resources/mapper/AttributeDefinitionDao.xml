<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.alibaba.robot.web.manage.mapper.AttributeDefinitionDao">

	<select id="selectByAttribute" resultType="com.alibaba.robot.web.manage.pojo.AttributeDefinition">
		select * from attributedefinition where type = #{type}
	</select>

	
</mapper>
